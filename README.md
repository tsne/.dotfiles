# .dotfiles

```shell
> EMAIL=<emailaddr> /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/tsne/.dotfiles/master/prepare-ssh)"

> git clone git@github.com:tsne/.dotfiles.git
> ./.dotfiles/install-macos
```
